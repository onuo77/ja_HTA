<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sample.dao.CartItemDao">
	
	<insert id="insertCartItem" parameterType="com.sample.vo.CartItem">
		insert into sample_cart_items
		(item_no, user_id, product_no)
		values
		(cartItem_seq.nextval, #{userId}, #{productNo})
	</insert>
	
	<update id="updateCartItem" parameterType="com.sample.vo.CartItem">
		update
			sample_cart_items
		set
			item_amount = #{amount}
		where
			item_no = #{no}
	</update>
	
	<select id="getCartItem" resultType="com.sample.vo.CartItem"> <!-- Dao에서 @Param을 작성한것은 parameterType 작성안함 -->
		select
			item_no			  no,
			user_id			  userId,
			product_no		  productNo,
			item_amount		  amount,
			item_created_date createdDate
		from
			sample_cart_items
		where
			user_id = #{userId}
			and product_no = #{productNo}
	</select>	
</mapper>